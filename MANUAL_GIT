Как сдавать ДЗ через гит:
Часть первая: создание репозитория (делаем только один раз в самом начале)
1. Открываем терминал. Переходим в корень проекта с Laravel
2. Выполняем команды (гит должен быть установлен предварительно):
    git init - создание локального репозитория
    git add . - добавление всех файлов в стэйдж
    git commit -m 'init' - первый коммит.
3. Идем на гитхаб (если нет аккаунта - создать, забросить публичный ключ чтоб не нужно было вводить пароль при пушах)
    https://github.com/
4. Создаем новый репозиторий (кнопка new)
   После создания репозитория нам выдадут http и ssh ссылку на него. Копируем ssh

5. Возвращаемся в терминал выполняем команду:
    git remote add origin ссылка_на_репозиторий
6. Пушим изменения из локального репозитория в гитхаб:
    git push -u origin master
После этого ветка мастер оказывается на гитхабе

Часть вторая: Выполнение дз. (делаем каждый раз для нового дз)
1. Работаем с локальным репозиторием. Убедимся что работаем с веткой мастер
(это будет основная ветка с актуальной версией):
    git status
  Если вдруг мы не на мастере переключаемся командой
    git checkout master
2. Создаем ветку для дз
    git checkout -b имя_ветки
    имя ветки может быть любое, например hw1 : git checkout -b hw1
3. Переключились на новую ветку, работаем к ней. Делаем свое дз.
4. Когда дз готово пушим его на гитхаб:
   git add .
   git commit -m 'hw1'
   git push -u origin имя_ветки

5. Идем на гитхаб. Заходим в репозиторий и переходим во вкладку Pull requests
   Там нажимаем на кнопку "new pull request"
   в base оставляем master в compare указываем ветку с заданием
   После этого нажимаем кнопку "Create Pull Request".
   После этого ПР будет создан ссылку на него можно скопировать из поисковой строки браузера
   и прикрепить к ДЗ.
   Можно сразу нажать кнопку "Merge" или подождать окончания проверки.
    Примечание: Если вы влили новую ветку недавно то предложение создать ПР
    вычветится в гитхабе автоматически
